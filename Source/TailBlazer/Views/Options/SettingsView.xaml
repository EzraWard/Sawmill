<UserControl x:Class="TailBlazer.Views.Options.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:options="clr-namespace:TailBlazer.Views.Options"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d">

    <!-- Settings page that aggregates the slide-over options -->
    <Grid>
        <ScrollViewer>
            <StackPanel Margin="12">
                <TextBlock Text="Settings" FontSize="20" FontWeight="SemiBold" Margin="0 0 0 8" />

                <!-- Reuse existing GeneralOptionsView. It will inherit DataContext from this control when created and bound in code. -->
                <options:GeneralOptionsView />

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
